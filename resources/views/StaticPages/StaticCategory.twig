{% extends 'Ceres::PageDesign.PageDesign' %}

{% block PageBody %}

    {% set categoryByRoute = services.category.get({{category)}}) %}

    {#{% set privacyPolicy = services.legalInformation.getPrivacyPolicy() %}#}

    <div class="container-max page-content">
        <div class="row">
            <div class="col-xs-12">
                <hr class="m-y-3">
                <div class="staticPages">{{ categoryByRoute.details.first.description | raw }}</div>

            </div>
        </div>
    </div>

{% endblock %}