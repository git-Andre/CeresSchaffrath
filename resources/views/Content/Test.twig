{% extends getPartial('page-design') %}
{% set itemMain = item.documents[0].data %}

{% block PageBody %}

    <div class="single container-max page-content">
        <h1>Hallo Andre {{ ziel }}</h1>

        {#<h4>{{ "now" | date('d/m/Y H:i', timezone="Europe/Paris") }}</h4>#}
        {#<h4>{{ "now" | date('F jS \\a\\t g:ia', timezone="Europe/Paris") }}</h4>#}


        {% set auction = auctions.auction.getAuctionForItemId(73000) %}

        {# ao Var-Dump #}
        {% include "CeresSchaffrath::VarDump" with { 'testDescription': 'auction', 'dumpData': auction } %}
    </div>


{% endblock %}
